version: '3.8'

services:
  mongodb:
    image: mongo:7
    container_name: mongodb_school
    environment:
      MONGO_INITDB_DATABASE: school-dashboard
    ports:
      - '27017:27017'
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data: